const grantBooking = () => {
  return 0;
};

const revokeBooking = () => {
  return 0;
};

const getAllBookings = () => {
  return 0;
};

const makeBooking = () => {
  return 0;
};

const cancelBooking = () => {
  return 0;
};

const getMyBookings = () => {
  return 0;
};

module.exports = {
  grantBooking,
  revokeBooking,
  getAllBookings,
  makeBooking,
  cancelBooking,
  getMyBookings,
};
